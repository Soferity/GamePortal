@if (AppValue.Platform == PlatformEnum.MacCatalyst || AppValue.Platform == PlatformEnum.iOS)
{
    <div class="wrapper">
        <canvas id="introduction-canvas"></canvas>
        <div class="introduction-canvas--overflow"></div>
    </div>
}
else if (AppValue.Platform == PlatformEnum.Android)
{
    <div id="particle"></div>
}
else
{
    <div id="world"></div>
}

@code
{
    protected override async Task OnInitializedAsync()
    {
        if (AppValue.Platform == PlatformEnum.MacCatalyst || AppValue.Platform == PlatformEnum.iOS)
        {
            await Include.Stylesheet("css/main/style.css");
            
            await Include.Script("js/main/script.js");
        }
        else if (AppValue.Platform == PlatformEnum.Android)
        {
            await Include.Stylesheet("css/main/styles.css");
            
            await Include.Script("js/main/jquery.min.js");
        }
        else
        {
            await Include.Script("js/main/three.min.js");
        }

        await GetRender();
    }
    
    private async Task GetRender()
    {
        await Task.Delay(DelayValue.Render);
        
        if (AppValue.Platform == PlatformEnum.MacCatalyst || AppValue.Platform == PlatformEnum.iOS)
        {
            //
        }
        else if (AppValue.Platform == PlatformEnum.Android)
        {

            await Include.Script("js/main/scripts.js");
        }
        else
        {
            await Include.Script("js/main/OrbitControls.js");
            await Include.Script("js/main/custom.js");
        }
    }
}